
//upon failure, display a hint as to why the level is not valid
function showFailureMessage(message){
	
	//TODO: create way for message to visually appear
	// maybe dont show hints by default, but add an option to "view hint"
	show_debug_message(message);

}